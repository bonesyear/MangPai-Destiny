# 第三轮审查 T0 · 运行时健壮性报告（2026-08-18）

> 范围：fuzz（随机 1900-2100×性别 全链）+ 边界特挑矩阵 + R5 遗留跨年翻档对拍（509 例）。
> 红线遵守：只测不改（零引擎/测试改动）；纯本地，`render_structured_reading(call_llm=False)` 零 API。
> 脚本：`/tmp/t0_fuzz.py` / `/tmp/t0_edge.py` / `/tmp/t0_yearflip.py`（易失，产出 JSON 同目录）。

## 1. fuzz 统计（N=800，seed=20260818）

链路：`calc_bazi_full → MangpaiEngine.compute_all → build_payload → render_structured_reading(call_llm=False)`。

| 指标 | 结果 |
|---|---|
| 样本 | 800（日期 1900-01-01~2100-12-31 均匀随机，时辰/分随机，性别池 男/女/None/''/未知/male，经度 73-135°E 随机） |
| 硬崩溃 | **0** |
| 异常慢（>5s/例） | **0**（均速 ~0.17s/例，全批 139s） |
| 异常输出（四柱畸形/核心键缺失/payload 空/render 坏） | **0** |
| 引擎静默模块失败（`_safe_compute` warning 捕获） | **0 次** |
| engine result 键数 | 恒 45（800/800 无抖动） |
| payload 键数 | 恒 38（与修批A 口径一致） |

结论：飞书集成硬阻塞项 = **零崩溃面**，通过。

## 2. 边界用例结果表

| 用例 | 观测 | 判定 |
|---|---|---|
| 晚子时 23:00-23:59 两口径 | 时柱两口径一致（一律次日干起子时）；日柱 next_day 归次日/same_day 归本日，分界严格落在真太阳时 23:00（lon=116.4 时 BT 23:00 未翻、23:30 翻） | ✅ 符合 F3 口径 |
| 早子时 00:00 | lon=120 时两口径日柱仍差一日（甲申/乙酉）——均时差 −2s 把 BT 00:00 压回前一日 23:59:58，±12h 跨日校正介入；子初/子正换日边界敏感带 ±1 分钟 | ⚠️ 记录：子夜 ±1min 不稳定带（两派各自自洽，非 bug；修复优先级 P2 备案） |
| 立春换年柱 | 2024-02-04 翻档于 16:00→17:00 之间（立春 16:27 吻合）；2024/2025 全 24 节 ±1 天 72 例全链零异常；2025 全部 12 节月支节前/后正常翻支 | ✅ |
| 大寒年柱 | 2024-01-20 全天年柱恒 癸卯、月柱恒 乙丑（换年只在立春，大寒不翻） | ✅ 符合盲派口径 |
| 闰月 | 引擎输入为纯阳历，农历闰月不构成输入面；闰六月/闰四月内阳历日期全链正常 | ✅ N/A（设计如此） |
| 年份极限 | 1900-01-01 00:00（己亥）✅；2100-12-31 23:59（庚申）✅；节气表覆盖 1900-2100 全闭区间 | ✅ |
| 界外年份 1899/2101 | `KeyError: 1899/2101` 直接抛（节气表越界）。不静默错排，但异常类型为裸 KeyError、无提示信息 | ⚠️ 修复优先级 P2：改抛带「支持范围 1900-2100」说明的 ValueError |
| 性别缺失/未知 | None/''/'未知' 一律静默按阴（逆排）处理，不报错不告警 | ⚠️ 修复优先级 P1（飞书集成前须定策略）：大运方向依赖性别，静默缺省=用户拿到反向大运而不自知；建议入口显式校验或 payload 标注 |
| 非法输入（2-30/月13/时25/分99/日0） | 全部干净 `ValueError` 带明确信息 | ✅ |
| 经度 None | `TypeError`（非校验性报错） | ⚠️ P2：入口校验 |
| 经度 −180/999 | 正常跑通，真太阳时随经度平移 | ✅（数值上行为正确） |

## 3. 跨年翻档对拍（R5 遗留，mock now() 2026-06-15 vs 2027-06-15）

509 例（train 294 + heldout 215，`_bazi_data` 同 blind_eval 口径，只跑引擎不评分）双年对拍：

| 指标 | 结果 |
|---|---|
| **yunsui_delta→tier 翻档** | **0/509**（caiming tier / tier_static / yunsui_delta / summary 全部跨年一致） |
| 官命 is_guanming / grade 翻转 | **0/509** |
| 职业 zhiye primary 翻转 | **0/509** |
| 判定域（caiming/guanming/zhiye/zuogong/gongliang/zhengfan/zeishen/muku/shensha/dayun）漂移键 | **0** |
| payload 逐例全同 / 有漂移 | 123 全同 / **386 有漂移（75.8%）** |
| 漂移键分布（544 种路径） | liunian_analysis 442、yunfan(liunian_fan/liunian_ji/sui_yun_liandong) 77、hunyin(结婚/离婚应期) 14、yingqi_subj 10、narrative 1 |
| 对拍异常 | 0 |

**结论**：翻档通道安全——`engine.py:493-500` 的「自动注入流年不入否决链」保护生效，终身判定三维跨年完全稳定。漂移 100% 限于「当下锚」岁运窗口域（自动构造的当年±1 流年切片），属设计内行为；但意味着**同一盘跨年交付给 LLM 的 payload 在流年/应期/婚姻应期键上不同**——交付不可复现性仅存于岁运域。若飞书侧要求完全可复现交付，按 R5 建议 4 注入 `as_of_year` 即可收口（非阻塞）。

## 4. 崩溃清单与修复优先级

**崩溃清单：空**（fuzz 800 + 边界 ~100 + 对拍 1018 引擎 run，零崩溃、零静默失败）。

| 优先级 | 项 | 现象/触发 | 建议 |
|---|---|---|---|
| P1 | 性别缺失静默按阴（逆排）处理 | gender=None/''/'未知' → 大运方向静默取逆排，无报错无告警 | 飞书集成前定策略：入口显式要求性别，或 payload 加「性别缺失·大运方向按缺省」标注 |
| P2 | 界外年份裸 `KeyError: 1899/2101` | 节气表覆盖 1900-2100，界外直接 KeyError 无说明 | 改抛带支持范围说明的 ValueError（一行 guard） |
| P2 | 子夜 ±1 分钟日柱敏感带 | 均时差 −2s 级残差使 BT 00:00 被压回前日 23:59:58，same_day 口径下日柱与 00:01 差一天 | 备案即可：真太阳时两派各自自洽，属历法固有边界 |
| P2 | city_lon=None 裸 `TypeError` | 入口未校验 | 入口校验一行 |
| 备案 | 闰月 | 引擎输入纯阳历，闰月非输入面 | N/A，文档注明即可 |

## 5. 与飞书集成的关系

硬阻塞面（崩溃/翻档）= **零**。集成前建议处理的只有 P1 性别缺省策略一项（语义正确性，非崩溃）；P2 三项为输入校验/文案级，可随集成批顺手带。

