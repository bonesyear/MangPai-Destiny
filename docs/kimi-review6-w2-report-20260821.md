# 第六轮审查 W2 · N 系列代码质量审查报告（2026-08-21，纯本地零 API，只审不改）

> 对象：N1 七维代码批 + N2/N2b 迭代修（`llm_channel.py` / `llm_prompt.py` 及配套）。
> 方法：当前代码为准独立复核 + r3 批数据（`output/llm_batch_20260821_n2_r3/batch_0_294.jsonl`）离线复算 + 七种原书全文书锚回查。

## 1. 七维 schema：五→七完整性

- **DIMENSIONS 扩展干净**。`llm_channel.py:33` 七维；L0（:86）/L1（:158）/L2（:278）/format_reading（:368）全部走 DIMENSIONS 循环，自动覆盖七维，无五维硬编码；N1 数字校验作用于整 blob，天然覆盖。三字段（conclusion/basis/confidence）七维结构一致，L0 逐维校验。
- 批跑/评测侧：N2 系列 `_n2_*.py` 已扩七维；`_llm_batch_analyze.py`/`_llm_batch_rescore.py`/`audit_v4.py` 维度无关或直 import DIMENSIONS，自动覆盖。旧 `_t3_*`/`_v3_*` 脚本绑死五维，属历史批归档（误用会漏计新维，仅提示）。
- **唯一实质漏改（测试侧）**：`test_f1_gate.py:26-28` `_five_dims()` 仍只造五维 mock，三处拒答窗测试在七维 L0 下 payload 自带「缺维度」违规——当前断言不检查附注故仍绿，但 mock 已不代表合法输出（P2）。
- 措辞过时（语义仍对）：`schools.py:38` 注释「LLM 五维不扩」（指 zinv，语义成立）；`llm_prompt.py:271`「五锚定行」字面仍准（锚定函数恰 5 个）；测试 mock 文案「mock 五维」无影响。

## 2. L2 两按维红线

**迁移维**（`_QIANYI_FORBID` 五词，裸 substring 无排除窗）：
- 书锚回查：出国/移民/海外/国外/外国**全部有书内明文断语锚**（zhongji:4179-4180「所以出国」、chuji:2053「都主出国」、lixiangxue:6571「移民外国」等）。即书内措辞上限本达「出国」级，引擎「迁移/远行」上限是**比书更严的人为收紧**——方向安全，禁词表与引擎上限自洽。
- 出境/离境：书内零命中零断语。按「零锚即禁」可补，但 LLM 无从书锚学来、实测 0 违规——补禁优先级最低（P2 可选）。
- 否定式「非出国」按设计计入违规（N2 r2 已验证），无排除窗是刻意从严，无误报窗需求（迁移维正文无合法含「外国」的场景）。

**相貌维排除窗**（`_xiangmao_exempt`，±1 相邻字判定）：
- 设计正确：±1 相邻字比财档/官命的 ±5 窗更准（±5 会放跑真结论词），注释已说明。美元/丑时/X丑干支三族放行逻辑无误，j=0/句尾边界安全（切片越界得空串不豁免=从严方向对）。
- 残留假阳族（低频无书锚场景）：**美金**（只豁免美元）、**小丑**、独立**丑月/丑年/丑日**（非干支对、非丑时）。相貌维正文出现概率低，列 P2。

## 3. 两锚定行 + 引用率口径 + sanitize

**分支逻辑**：`_qianyi_anchor`/`_xiangmao_anchor` 有信号/无信号分支均正确；无 marker 盘「无显著相貌特征」一句的触发 = 五主线 hit&desc 全空且眼象 (bing|ding|gui)&desc 不成立——与 SCHEMA「各线均未命中」语义一致（含 hit 但 desc 空的边界，从严方向对）。迁移无信号 8/30 例写「数据不足」而非模板「无迁移信号」——basis 空数组合规、非断言迁移，纯措辞偏离模板（P2）。

**相貌引用率 251/267 口径裁定（风险 #9，离线复算 294 例）**：
- 16 例 miss 根因**全部同一边界**：盘中独癸（`yanxiang.gui=True`）无丙/丁——引擎 `xiangmao.py:150-161` 组 desc 只认 bing/ding/eye_full，独癸 desc='' → 锚定按无 marker 分支输出；而统计口径 `output/_n2_analyze.py:53-58` `_has_xiangmao_marker` 只看布尔不看 desc，计为「有 marker」。
- **锚定未输出 16 / LLM 漏引 0**。16 例 LLM 全部 basis=[] + 「无显著相貌特征」，行为 100% 合规。对齐口径后实际引用率 = **251/251 = 100%**。
- 属性：统计口径 bug，非生产代码 bug（P1，修 `_n2_analyze.py` 口径即可；引擎独癸无书锚 desc，保持沉默是对的）。
- 逐例：b67-生例四企业家、cj-6155、cj-农民、cj-国务院总理、cj-工薪、cj-破财己卯年破大财、cj-贫一生受穷、cj-运动员、famous-汉武帝、famous-王阳明、gj-泄身艺匠、reg67-甲子地矿局长、yx-2658、yx-8721、yx-宰相、zj-公司职员。

**_xm_sanitize 语义安全域（风险 #4）**：
- 当前**安全**：全部 desc 中「漂亮」仅出现在 `xiangmao.py:111` 秀气线性别分流语（「女看秀气漂亮倾向」），确为秀气语境；替换顺序正确（先「秀气漂亮」→「秀气」，「女看秀气倾向」无语义错误）。
- 脆弱点：替换是**静默语义改写**——未来引擎任何 desc 新增非秀气语境的「漂亮」（如引书「家庭漂亮」类）会被无声改成「秀气」且 sanitize 掩盖问题不告警。F-N2-1 引擎侧修复（:111 改「女看秀气倾向」）落地后，此函数即成死代码，**届时应连函数一起删**（P2）。

**unemployed 直述锚（F-V3-1）**：`_zhiye_anchor` 改动正确——unemployed/laborer 桶明令如实直述、禁改述「无明确职业倾向」、禁另给安稳就业建议；N2 r2/r3 zhenbao-23a 两轮行为确认。label 解析（primary_label → _BUCKET_LABELS 兜底）覆盖全部引擎桶，无空 label 路径。

## 4. 复合词族补禁清单（书锚判定，风险 #6）

七种原书全文回查：**甜美/标致/水灵/清秀/端庄全部书内零锚**（连同俊美/秀美/优美/柔美亦零锚，N2b 裁定属实）。书内相貌评价词上限=漂亮/美貌/秀气（仅「女命秀气主漂亮」语境）/好看/曲线好/性感（zhongji:3982）。

按 N2b「零锚即禁」裁定同构处理，五词全部该禁。落点：
- **甜美**：含「美」，校验器单字扫描已覆盖；仅需 prompt 点名完整性。
- **标致/水灵/清秀/端庄**：不含美/丑/帅字，**校验器抓不到、prompt 未点名——真漏网**（P1）。补法同 N2b 双轨：prompt SCHEMA+锚定禁令点名词族（生成侧主防）；`_XIANGMAO_FORBID` 扩此四词（校验器兜底，词级匹配无单字误报问题，无需排除窗）。

## 5. P0/P1/P2 清单

- **P0**：无。
- **P1**：
  1. 相貌引用率统计口径：`output/_n2_analyze.py:53-58` `_has_xiangmao_marker` 改「hit 且 desc 非空」（或补 eye_full 同口径），与锚定对齐；对齐后 251/251。
  2. 复合词补禁：prompt 点名 + `_XIANGMAO_FORBID` 扩 标致/水灵/清秀/端庄（甜美已被单字「美」覆盖，prompt 补点名即可）。
- **P2**：
  1. `test_f1_gate.py:26-28` `_five_dims()` 改从 DIMENSIONS 生成七维 mock。
  2. `_xm_sanitize` 脆弱性备案：F-N2-1 引擎侧修复落地后连函数删除；删除前若引擎 desc 新增「漂亮」会被静默误改。
  3. 相貌排除窗残留假阳族：美金/小丑/丑月（低频，出现再补）。
  4. 迁移无信号「数据不足」措辞偏离模板 8 例：锚定补「禁写数据不足」或如实率判定放宽。
  5. `schools.py:38` 注释「五维」字样同步七维口径。
  6. 迁移禁词可选补 出境/离境（书内零锚，实测零违规，优先级最低）。
- **确认属实但已有认领**：feishu 引擎直出降级路径五段不含迁移/相貌（w1 风险 #2，非本批新发现）。

## 6. 汇报（≤300 字）

W2 审查完成（纯本地零 API，只审不改）。七维 schema 扩展干净：L0/L1/L2/N1 全走 DIMENSIONS 循环自动覆盖，生产代码零五维漏改；唯一实质漏改在测试侧（test_f1_gate 五维 mock）。两红线判定：迁移五禁词全部有书内断语锚，引擎「迁移/远行」上限比书更严=安全收紧，出境/离境书内零锚可选补；相貌 ±1 相邻字排除窗设计正确，残留美金/小丑/丑月低频假阳族。251/267 口径裁定：16 例全为「独癸 gui=True 但 desc 空」——锚定按无 marker 输出、统计口径误计有 marker，LLM 漏引 0 例，对齐口径后实为 251/251=100%（P1 修统计脚本）。_xm_sanitize 当前安全（「漂亮」仅 xiangmao.py:111 秀气语境一处），但属静默改写，F-N2-1 修复后应连函数删。复合词族：甜美/标致/水灵/清秀/端庄书内全部零锚，按 N2b 裁定同构该禁——标致/水灵/清秀/端庄校验器抓不到=真漏网（P1 双轨补禁）。F-V3-1 失业直述锚改动正确。P0=0、P1×2、P2×6，修批 G 另排。
