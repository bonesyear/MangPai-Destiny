# Kimi 任务：LLM 通路 · prompt 迭代批（4 调优方向 + 复测）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/llm-batch-report-20260818.md`（批量校验报告：L1 40.2% 超标，两类主因=编造子键 125 条 + 数组越界/类型错 91 条；N1 假阳「55岁」76/80 条；L2 财档越限 58 条）+ `/root/metaphysics/docs/knowledge-base.md`
2. 本批 = **prompt/校验器迭代**（只改 llm_prompt.py/llm_channel.py，引擎零改动）+ **294 例复测**
3. 汇报 300 字内

## 任务（4 调优方向全落地）
1. **basis 逐字照抄 + 数组禁带下标**（最大杠杆，治 216 条大头）：
   - prompt 明确：引用特征必须逐字照抄特征键名；数组只能引用整个数组或明确字段名，禁止 `juxiang[7]` 式下标
   - 校验器 L1：数组下标访问直接判违规（validate 模式）
2. **财档枚举锚定**：prompt 财运档位词只允许五档（巨富/富/小康/平/贫）之一且必须取自 `caiming.tier_static/tier` 原值；显式注明「金额档≠财命档」（LLM 把功量亿级误升格）
3. **大限边界入年龄白名单**（一行）：55 岁式系统性假阳——白名单收大限边界
4. **L2 否定窗扩后两字**：validator-side（「官命否决/无缘」类，扩到后 2 字符）

## 复测
- 294 例全量复测（validate=mark），同批量校验口径
- 达标判定：L0≈0 / N1<2%（白名单后）/ **L1<5%**
- 不达标 → 继续分析剩余违规分布（迭代 2 的方向）

## 红线
- 引擎零改动（只动 llm_prompt.py/llm_channel.py）
- 只吃 trainset（不碰留出集）
- LLM 输出不落 compute_all dict

## 验证
1. pytest 全绿（test_llm_channel 9 测适配新规则）
2. 复测达标判定表
3. 成本实测（¥/轮）

## 汇报（300 字内）
4 项改动要点 + 复测达标判定（L0/N1/L1 实测率）+ 剩余违规分布 + 成本 + 是否可进正式通道
