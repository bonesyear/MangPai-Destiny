# Kimi 任务：LLM 通路 · trainset 294 例批量校验

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md` + 上一批 MVP 成果（llm_channel.py 三层校验 validate=mark/reject/off、达标线 L1<5%/N1<2%）
2. 本批 = **294 例全量批量自动校验**（纯评估不改引擎；prompt 调优方向分析）
3. 汇报 300 字内

## 任务
1. **批量跑**：trainset 294 例全量过 llm_channel（validate=mark 模式，出违规标记不阻塞）
   - 注意成本：~¥8/294 例；可分批（如 3 批 × 98 例）避免超时
   - 记录每例：tokens/耗时/校验结果
2. **达标判定**：
   - L1 违规率（basis 路径无出处）< 5%
   - N1 数字违规率（LLM 数字与引擎不一致）< 2%
   - L0 schema 解析失败率（应为 0 或接近）
3. **违规分析**（不达标才需要深挖，达标也列分布）：
   - 违规类型分布（哪个维度/哪类路径）
   - 典型案例 3-5 个（违规内容 + 特征上下文）
   - **prompt 调优方向**（只吃 trainset——哪些特征需要更明确的 prompt 指引/哪些枚举值域要收紧）
4. **产出报告**：达标判定表 + 违规分布 + 调优方向 + 成本实测

## 红线
- **纯评估**：不改引擎、不改 prompt（调优方向分析可以给，落地下一批）
- 只吃 trainset（不碰留出集）
- LLM 输出不落 compute_all dict（维持）

## 验证
- pytest 全绿（引擎零改动确认）
- 报告落盘（违规明细可追溯）

## 汇报（300 字内）
达标判定（L1/N1/L0 实测率）+ 违规分布 + 调优方向要点 + 成本实测 + 是否可进正式通道
