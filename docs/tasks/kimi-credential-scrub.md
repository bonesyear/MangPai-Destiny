# Kimi 任务：清除入库的真实凭证 + 全仓凭证复扫（安全整改）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md`
2. 本批 = 删除泄漏文件 + 凭证复扫 + 暴露范围评估（**只出建议，不做 git 历史重写**）
3. **报告与 stdout 一律不得出现任何 key 明文**（用 `ark-xxxx<REDACTED>` 形式；这是安全批的红线）
4. 汇报 400 字内

## 背景（主会话已核实）
`docs/cc-volcengine-config-backup.md:9` 含**真实火山引擎 ARK API key**（`export ANTHROPIC_API_KEY=ark-xxxx<REDACTED>`），已入库且位于**仓库初始提交 `878f3ce`**（2026-07-15）→ 推至公开 GitHub 已两个月。全仓扫描确认**真凭证仅此一处**（其余命中 `.env.example`/`feishu/README` 均为占位符）。

用户已决定：**删除该文件**（它是 CC/火山配置备份 = 本机工具环境内容，按仓库卫生约定「工具环境内容不进仓库」本就不该入库）。用户将自行轮换该 key（轮换后历史中的旧 key 失效，故本批**不做历史重写**）。

## 任务 A：删除泄漏文件
1. `git rm docs/cc-volcengine-config-backup.md`
2. **引用检查**：全仓 grep 该文件名/`volcengine`/`cc-volcengine`，确认无文档/代码引用（若有，一并处理并说明）
3. 确认删除后工作区与索引状态正确

## 任务 B：全仓凭证复扫（含未跟踪文件）
扫描范围与模式（**输出必须打码**）：
- `git grep`：`ark-[A-Za-z0-9]{6,}`、`sk-[A-Za-z0-9]{16,}`、`(API_KEY|TOKEN|SECRET|PASSWORD)\s*=\s*[A-Za-z0-9_-]{12,}`、`ghp_`、`gho_`、`AKIA`、`BEGIN.*PRIVATE KEY`
- **未跟踪文件**（`git status --porcelain` 全量 + `output/` 目录）：同样模式扫一遍（这些可能正要入库）
- **区分**：真凭证 vs 占位符（`xxx`/`your-xxx`/`<set>`/示例）
- 每个命中给：文件:行号 + 判定（真凭证 / 占位符 / 需人工确认）+ **打码后的片段**

## 任务 C：暴露范围评估（只报告，不实施）
1. 该 key 在 git 历史中的**完整暴露区间**（首次进入 → 至今；确认是否仅初始提交引入、后续有无变更）
2. 该文件是否曾出现在任何**非初始提交**（如被修改过）
3. **建议**：在「用户即将轮换 key」前提下，是否需要 `git filter-repo`/BFG 历史重写？（给结论 + 代价，含对 fork/clone 的影响；本批不执行）
4. 顺带核查：仓库里是否还有其他「本机工具环境内容」入库（`docs/` 与根目录下类似备份/配置类文件），列出清单供主会话转呈用户

## 任务 D：验证（DoD）
- [ ] A：文件已 `git rm`；引用检查零残留（或已处理）
- [ ] B：扫描完成，命中清单完整（真凭证须为 0——若不为 0，**停下并在报告首行明确标注**）
- [ ] C：暴露区间/历史重写建议/工具环境内容清单三项齐全
- [ ] **六件套**：pytest 全绿 + verify 432/70/64/20 + blind vs `snapshots/20260918_t1.json` 零翻转 + 分层两件套通过（本批为文档删除，预期零影响，但须实证）
- [ ] `git status --short` 核对改动范围（应仅该文件删除 + 你新增的报告）
- [ ] 汇报 400 字内

## 红线
- **任何输出不得含 key 明文**（含报告文件、commit message、stdout）
- **不做 git 历史重写**、不 force push（用户未批准；本批仅告知建议）
- 不调外部 API；不动引擎判定
- 可自行 commit（**不 push**，push 由主会话验收后执行）
