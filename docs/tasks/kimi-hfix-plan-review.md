# Kimi 任务：检视 H-fix 工作计划（纯规划评估 + 自报模型版本）

## ⚠️ 执行指引
1. **开头先自报身份**：说明你当前实际使用的模型 ID / 版本（用于确认 k3 是否生效——此前发现会话实际走 K2.7）
2. **先读** `/root/metaphysics/docs/tasks/h-fix-workplan-20260825.md`（H-fix 8 批工作计划）+ `/root/metaphysics/docs/tasks/codehygiene-fix-backlog.md`（498 处问题池 + H11 施工图 + H13 分批规划）
3. 本批 = **纯规划检视**（不执行修复；不调外部 API；读文件 + 评估）
4. 汇报 400 字内

## 检视内容
1. **计划合理性**：
   - H-fix 8 批的依赖拓扑是否正确（顺序有无倒置/遗漏）
   - 每批的"关键纪律"是否充分（吞改抛的错误注入策略、拆分的等价性保障、死代码的 prompt-only 确认）
   - 是否有 H1-H13 发现但未纳入任何 H-fix 批的遗漏项
2. **优先级判断复核**：
   - 🔴 三批（import 崩溃面/异常策略/原子写）是否确实该先行
   - 🟡🟢 批次的后置是否合理
3. **工作量与批次粒度**：
   - 每批规模是否可控（Kimi 会话风控：单批 ≤30 文件 / 避免超长会话）
   - 是否需要进一步拆分或合并（如 115 处裸 except 是否应再分小批）
4. **风险预案复核**：
   - "吞改抛"暴露新崩溃面的应对是否充分
   - 是否有回滚/分批验证方案
5. **模型切换影响**（K2.7 → k3）：
   - 之前批次在 K2.7 下完成——H-fix 用 k3 跑是否需要额外的"旧代码熟悉"步骤
   - 是否需要轻量抽查（计划里列为可选）
6. **补充建议**：你认为计划还缺什么（如是否需要先做一个"错误注入测试框架"作为 H-fix-2 的前置）

## 产出
- 检视报告（建议写 `/root/.claude/projects/-root-metaphysics/memory/kimi-h-fix-plan-review-20260828.md`）
- stdout 汇报 400 字内：模型自报 + 检视结论（合理/需调整项）+ 遗漏项 + 优先级意见

## 红线
- 只检视不改计划文件（可以提修改建议）
- 不执行任何修复；不调外部 API
