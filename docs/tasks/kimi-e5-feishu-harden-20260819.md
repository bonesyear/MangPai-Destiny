# Kimi 任务：修批 E5 · 飞书加固（U2 P2 余项）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md` + U2 报告（`docs/kimi-review4-u2-feishu-20260819.md` P2 六项：①_seen_mids 超 2000 全清重放窗口重开（已复现）②token 刷新无锁并发重复刷新（8 线程刷 8 次）③静默错解三例（123:45→23:45 出报告/秒位丢弃/「四柱」触发词抢占阳历输入）④VT 未配零校验（E1 已修，跳过）⑤500 回显内部错误 ⑥单线程 server 慢连接阻塞）+ 修批 E 规划（E5：飞书加固，后置可选——量级上来再做，但既然开工就顺手清）
2. 本批 = **E5 飞书加固**（mangpai/feishu/ 可改，引擎零改动，纯本地零 DeepSeek）
3. 汇报 300 字内

## 任务（六项，④已修跳过）
1. **重放窗口加固**（P2-1）：_seen_mids 超 2000 全清 → 改为**去重不丢窗口**（如 LRU/带时间戳滚动清理，过期消息才清）
2. **token 刷新锁**（P2-2）：并发刷新竞态 → 加锁（线程锁，刷新期间其他线程等待复用新 token）
3. **静默错解修**（P2-3 三例）：
   - `123:45` → 23:45 出报告（时间解析边界——123 分钟非法应报错而非静默截断）
   - 秒位丢弃（用户输入秒应接受或明确报错）
   - 「四柱」触发词抢占阳历输入（触发词判定优先级——阳历日期在前则走阳历）
4. **500 回显**（P2-5）：异常响应不回显内部错误（str(e)）——改通用错误信息 + 日志记详情
5. **HTTPServer 上限**（P2-6）：单线程慢连接阻塞 → 线程数上限/超时配置（README 注明参数）
6. 哨兵：test_feishu 补测试（重放窗口滚动/token 锁/时间解析边界/四柱抢占/500 回显）

## 红线
- 引擎零改动；正常路径行为不变
- 不连真实飞书（mock）；不调 DeepSeek

## 验证
1. 哨兵红绿（新增测试）
2. pytest 全绿（767+新增）
3. mock 冒烟（并发 token 刷新 8 线程→1 次刷新；重放窗口滚动）

## 汇报（300 字内）
六项实现 + 哨兵 + pytest + mock 冒烟
