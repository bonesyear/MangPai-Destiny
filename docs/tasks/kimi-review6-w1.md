# Kimi 任务：第六轮审查 W1 · F1+F3 十一项代码审查（纯本地）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md` + 第六轮方案（W1：F1+F3 十一项逐项审——免责声明落点/lark_md 三符转换/死亡词 reject 逻辑/max_tokens 8192/_self_check 人民币/L2 误报窗）+ F1+F3 报告（commit f3d3e5e）+ 九项风险面（尤其 #1 reject L0/L2 层级混用、#3 降级路径免责与 prompt 外泄、#7 lark_md · 渲染）
2. **独立判断纪律**：以当前代码为准
3. 本批 = **W1 F1+F3 代码审查**（只审不改；纯本地零 API）
4. 汇报 300 字内

## 任务（F1+F3 十一项逐项审）
1. **免责声明**（P0 修复项）：两路径（formatter 引擎直出尾 + format_reading LLM 叙述尾）+ HELP——落点正确？重复？缺失场景？
2. **lark_md 三符转换**（`- `→`· `、`> `→粗体、`---` 删除）：转换完整性（全仓还有漏网 `- `/`> `/`---`？）；`·` 在飞书的实际渲染；粗体嵌套问题
3. **死亡词 reject 逻辑**（重点，风险 #1/#2）：
   - **L0/L2 层级归属**：llm_channel.py:434 判 L0、枚举在 _l2_enum 标 L2——混用是否导致校验器状态不一致（L0 挂了但 L2 计数漏）
   - reject 边界：误杀合规句？漏杀？（≥8 合成例验证：正常句/含死词句/近似词句/英文词）
   - reject 降级后引擎直出**仅五段不含迁移/相貌**（风险 #2——七维数据丢失？）
   - LLM 不可用降级路径是否带免责 + prompt 外泄（风险 #3）
4. **max_tokens 8192**：截断风险（七维输出更长——8192 够？）；成本影响
5. **_self_check 人民币口径**：价表与 _PRICE 一致（风险 #8）？
6. **L2 误报窗**：拒答句豁免窗边界（标点/换行/窗宽——风险 #5）
7. **isinstance/脱敏**：bot 层加固完整性

## 红线
- 只审不改（P0/P1/P2 清单，修批 G 另排）
- 纯本地零 API

## 产出
1. 十一项逐项审查表（含风险 #1-3/#5/#7/#8 判定）
2. reject 边界合成例测试结果
3. P0/P1/P2 清单
4. 汇报 300 字内
