# 工作计划存档（2026-08-25 · 配额冻结期）

> **状态**：Kimi 配额冻结，预计**下月中旬（~9-15）恢复**。恢复前不派任何 Kimi 任务。
> **待用户通知后动工。**

## 一、H-fix 修复序列（8 批，依赖拓扑，规划已就绪）

> 完整问题池：`docs/tasks/codehygiene-fix-backlog.md`（498 处去重 ~45 核心项）
> 施工图：H11 的 `_safe_compute` 37 模块对照 + 三大函数拆分方案 + H13 分批规划

| 批 | 内容 | 优先级 | 关键纪律 |
|----|------|--------|---------|
| **H-fix-1** | import/typing 崩溃面（Optional/Any/Tuple——部署机 3.11 就崩，已实测） | 🔴 先发 | 一行级 |
| **H-fix-2** | 异常处理策略批（115 处裸 except 系统性改造——按 _safe_compute 施工图） | 🔴 | 吞改抛须错误注入测试 + 盲测零翻转 |
| **H-fix-3** | 原子写批（12 处——基线误写=评估污染） | 🔴 | --write-baseline 加原子写 |
| H-fix-4 | 死代码/重复清理（_check_pair 四文件/十神十余处/shipaige 死数据/8 个诊断脚本归档） | 🟡 | 删前确认 prompt-only 字段 |
| H-fix-5 | 大函数拆分（gongliang 957 行/detect_relations 850 行/compute_all 490 行——按 H11 方案） | 🟡 | 函数级哨兵 + 六件套，禁止顺带改口径 |
| H-fix-6 | selectors 契约（engine-key 自动契约测试） | 🟡 | 新键漏登记静默丢信号 |
| H-fix-7 | 评测框架统一（_n2/_t3/_v3/_w4/_w5 合并，可选） | 🟢 | 防口径漂移 |
| H-fix-8 | 文档/基线同步（README 23 例过期/MANUAL 4 例/快照 LATEST 指针） | 🟢 | 纯文档 |

## 二、模型配置修复（优先于 H-fix-1）

- **问题**：config `default_model = "kimi-k3"` 但实际会话走 `kimi-for-coding`（K2.7）——之前批次在 2.7 下运行
- **修复**：确认下次会话实际切到 k3（查 CLI 0.39.0 为何走 2.7——会话选择/升级改默认）；H-fix 全批用 k3 跑（新模型改旧代码=天然再审视）
- **轻量抽查（可选）**：H-fix-1 顺带抽 3-5 关键模块让 k3 扫（确认无 2.7 特有缺陷模式）

## 三、配额恢复后动工顺序

```
① 模型配置修复确认（k3 实际生效）→ ② H-fix-1（import 崩溃面）→ ③ H-fix-2 → ④ H-fix-3
→ ⑤ H-fix-4/5/6（下一 milestone）→ ⑥ H-fix-7/8（可后置）
```

## 四、其他挂起（不受配额影响，随时可做）

- 群聊 @bot 验证（后置，需真实群）
- 独立 webhook 部署（可选）
- 跨流派扩展（长期）
- 原著索引已在 KB §0 生效（不依赖 Kimi）
