# Kimi 任务：H-fix-5 · 大函数拆分批（🟡 最后一批）

## ⚠️ 执行指引
1. **先读**：`docs/tasks/h-fix-workplan-v2-20260917.md` + `docs/tasks/codehygiene-fix-backlog.md` 的 **H11 节**（三大函数拆分方案——本批的执行依据）+ **H-fix-6 节**（契约测试已就位，锁 payload 41 键 + engine-key 归类）+ **H-fix-4b 节**（detect_relations 已注册表化，本次只做函数拆分）+ **H-fix-4c 节**（依赖图 + 拆分顺序建议）
2. 本批 = **H-fix-5（🟡 大函数拆分——H-fix 序列最后一批功能批）**
3. 汇报 400 字内

## 三个目标函数（按 H11 方案）

### 1. `subjective/gongliang.py` — `analyze_gongliang`（957 行）
H11 方案 → **5 子函数**：①输入准备 ②位置集合 ③计分规则 ④封顶方向 ⑤结果装配
- ⚠️ 此函数是**层功量化的核心**（L1-L4 判定）——拆分必须逐字等价

### 2. `objective/zuogong_detect.py` — `detect_relations`（850 行，4b 已注册表化）
H11 方案 → 6 组循环已统一（4b 完成），本次按语义拆分：六合/暗合/冲/刑/害/破 各成独立子函数 + 主函数编排
- 生用/三合/墓用 保留独立子函数（H11 既定）

### 3. `engine.py` — `compute_all`（490 行）
H11 方案 → **5 阶段**：①objective 基础 ②zuogong 衍生 ③运岁 ④subjective 领域 ⑤summary
- ⚠️ **契约测试（H-fix-6）会锁住键归类**——拆分时若键集合/结构变动会立刻红

## 拆分纪律（本批红线）
1. **逐字等价**：拆分只做"代码搬家 + 函数抽取"，**不改任何判定逻辑、阈值、分支顺序**
2. **禁止顺带改口径**（v2 计划红线）——发现的问题记录不修
3. **函数级哨兵**：每个新子函数加针对性单测（先红后绿——对未实现子函数应先失败）
4. **中间状态可验证**：每拆一个函数就跑六件套（或至少 pytest + blind），不带红前进
5. **签名设计**：子函数参数尽量显式传递（避免依赖闭包/全局），便于测试

## 验证（DoD）
- [ ] 三个函数拆分完成（行数下降；每个子函数有单测）
- [ ] **正常路径输出逐字节不变**（heldout+trainset 全量 engine dict 深比较）
- [ ] **契约测试通过**（payload 41 键 / engine-key 归类不变）
- [ ] 六件套全量：pytest 全绿 + verify 全项 + **blind vs `snapshots/20260918_hfix6.json` 零翻转零抖动**
- [ ] `scripts/check_layering.py` 通过（拆分不得引入跨层依赖）
- [ ] 3.11 + 3.14 import 冒烟

## 分诊纪律
- 拆分中若发现既有 bug → 记录 backlog + **不在本批修**（拆分与修复不混）
- 不带红前进

## 产出
- 三个函数拆分 + 子函数单测
- backlog 追加「H-fix-5」节（含拆分后的结构说明 + 发现的待议问题）
- 汇报 400 字内：三函数拆分结果（行数变化/子函数数）+ 哨兵数 + 等价性验证 + 六件套

## 红线
- **逐字等价（判定逻辑零改动）**
- 禁止顺带改口径
- 不调外部 API
