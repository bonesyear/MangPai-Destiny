# Kimi 任务：代码卫生审查 H11 · subjective 深度补充批（复杂函数拆分点 + schools 保护链 + 接线面）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md` + 代码卫生规划 + H1-H10 报告（`docs/tasks/codehygiene-fix-backlog.md` 已有 P0×108——本批**重点查同型残留**：裸 except/原子写/死 shim/口径漂移）+ 收工终态
2. 本批 = **代码卫生 H11 · 深度补充批**（只审不改；纯本地零 API；针对前几批标记"需深挖"的点）
3. 汇报 300 字内

## 审查对象（深度补充——前几批发现的复杂点精审）
1. **gongliang.py:226 957 行函数**（H2 标记）：逐段结构分析——拆分为多个子函数的建议方案（拆分点/依赖关系/可测性）
2. **detect_relations 850 行 + 6 组 O(n²) 复制循环**（H1 标记）：逐组分析——6 组循环的差异点/合并方案
3. **compute_all 490 行**（H8 标记）：分段结构分析——拆分方案
4. **schools.py selectors 机制深审**（H4/H8 标记）：selectors 注册/过滤/保护链的机制健康度——新增键的登记流程是否防呆（未来新模块接线的 check）
5. **_safe_compute 模式审计**（H8 标记 P0）：engine 里每个模块的 _safe_compute 包裹清单——哪些模块异常被吞、哪些正确传导——**输出"模块→异常处理方式"对照表**（修复批的施工图）

## 审查维度
1. **复杂函数拆分方案**（前 3 个）：不是简单标记"太长"，而是给出**可执行的拆分建议**（子函数边界/参数/返回值/调用链——修复批 H-fix 直接可用）
2. **selectors 机制**：登记防呆（新键漏登记会怎样？）、过滤逻辑、保护链的脆弱点
3. **_safe_compute 对照表**：全模块清单（哪个模块被包裹/裸跑/异常吞/传导）——修复批施工图
4. 同型残留复查

## 产出
1. 拆分方案表（函数→建议子函数边界+理由）
2. _safe_compute 模块对照表
3. selectors 机制评估
4. P0/P1/P2 统计（本批新发现）
5. 追加写入 `docs/tasks/codehygiene-fix-backlog.md`（H11 节）
6. 汇报 300 字内

## 红线
- 只审不改；纯本地零 API
- 拆分方案是"建议"——实际拆分留给修批 H-fix（书锚纪律下验证不能丢）

## 汇报（300 字内）
拆分方案要点（3 个大函数）+ _safe_compute 对照表概况 + selectors 机制 + P0/P1/P2 统计
