# Kimi 任务：清除 key 前缀残留（打码加固）

## ⚠️ 执行指引
1. 承接上一批（凭证清除 `7e8811f`）。本批 = **把残留的 key 前 8 位前缀也彻底打码**
2. 输出（含报告/commit message/stdout）**不得出现任何 `ark-` 后的真实字符**
3. 汇报 200 字内

## 背景
上一批删除泄漏文件后，全仓复扫时**遗漏了「部分明文前缀」**：任务书与报告中复述该 key 时保留了真实前 8 位（形如 `ark-` + 8 位真实字符 + 省略号 / + `<REDACTED>`）。8 位前缀不足以利用，但违反「报告一律不得含明文」的自定红线，须清零。

## 任务
1. **全仓扫描**（git 跟踪 + 未跟踪 + `output/`），找所有 `ark-` 后跟任意真实字符的命中：
   - `git grep -nE "ark-[A-Za-z0-9]"`（当前树）
   - 同样模式扫未跟踪文件与 `output/`
   - 排除：纯占位符（`ark-xxx`、`ark-your-...`）——这类保留
2. **逐处打码**：把真实前缀替换为不含任何真实字符的形式，如 `ark-xxxx<REDACTED>`
   - 已知至少两处：`docs/tasks/kimi-credential-scrub.md`、`docs/kimi-credential-scrub-report-20260919.md`
3. **顺带自查同类问题**：其他凭证模式是否也有「部分保留」（如 `sk-` 后保留真实字符、`ghp_` 后保留字符）——一并打码
4. **验证**：
   - `git grep -nE "ark-[A-Za-z0-9]"` 复扫：仅剩占位符形式（`ark-xxx`）或零命中
   - 未跟踪文件同扫
   - 全量 pytest 1129+1xf 复跑（文档改动，预期零影响，须实证）
5. **提交**（不 push）：commit message 不得含明文

## 红线
- **不得出现任何真实 key 字符**（含前缀位、末位、任何中间片段）
- 只改文档，不动代码/引擎
- 不做 git 历史重写（历史里这两处随 key 轮换后无关；本批只清当前树）
- 汇报 200 字内（命中处数 + 打码方式 + 复扫结果）
