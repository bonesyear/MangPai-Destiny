# Kimi 任务：代码卫生审查 · 规划方案（49 模块实现质量全覆盖，纯规划）

## ⚠️ 执行指引
- 纯规划：方案写归档 `/root/.claude/projects/-root-metaphysics/memory/kimi-codereview-plan-2026-08-25.md`，stdout 300 字摘要
- **只出方案不执行**
- **先读**：`/root/metaphysics/docs/knowledge-base.md` + 收工终态（`docs/remaining-tasks-20260822.md`）+ 第七轮元判断（`docs/review7-assessment-20260822.md`：行为级审查可停、事件触发；**代码卫生审查是独立缺口**——R 轮死数据/U 轮代码审只有定向覆盖，49 模块实现质量系统性全覆盖从未做过）

## 背景与定位
- **目的**：代码卫生审查（实现质量线）——审"代码健不健康"，区别于行为级审查（输出对不对，已覆盖可停）
- **血泪教训**（为什么值得做）：V6 Tuple 未导入（import 级崩溃，≤3.13 环境全挂）、F4 签名错配（TypeError 被 except 吞）——都是**行为审查抓不到、运行时才炸**的代码级缺陷
- **审查对象**：mangpai/ 全部模块——objective（bazi_calc/zuogong_detect/engine 等）+ subjective（11 个判断模块 + llm_* + qianyi/xiangmao/zinv）+ feishu 包 + scripts + tests 基础设施
- **方法参考**：Kimi code-review skill 双轴（Standards=Fowler 坏味道基线 / Spec=忠实实现）——审查标准用 Standards 轴为主（实现质量），Spec 轴已有六轮覆盖不重复

## 请设计
1. **审查维度**（每模块逐项）：
   - 重复逻辑（跨模块重复/模块内重复——可提炼未提炼）
   - 复杂度（函数过长/分支过深/圈复杂度）
   - 命名（误导性命名/与书理术语冲突）
   - 异常处理一致性（裸 except/TypeError 未捕获/静默失败）
   - 死代码（未引用函数/死参数/死分支/冗余条件）
   - 隐藏边界假设（硬编码/魔法数字/隐含依赖）
   - 类型与 import 卫生（未导入引用/延迟绑定风险——Tuple 类）
2. **批次划分**（老规矩：依赖拓扑 + 每批可独立验收）：
   - 按模块群分组（如 objective 核心批/主观判断批/LLM 通道批/飞书批/测试基建批）
   - 每批：对象清单/审查维度/产出格式（问题表：文件:行号/问题类型/严重级 P0-P2/修法建议）/验收标准
3. **发现分级**：P0（运行时崩溃面=阻塞）/P1（必修）/P2（卫生建议）
4. **配额预估**（纯本地零 API——吃 Kimi 配额）
5. **执行序 + 修复衔接**（发现→修批 H？与既有修复流程衔接）

## 注意
- 只出方案不执行
- 每批规模可控（Kimi 会话风控：单批 ≤30 文件或 ≤1 模块群）
- 方案可执行（任务文件能直接写）

## 输出（写归档，300 字摘要）
维度清单 + 批次划分（每批对象/产出/验收）+ 分级 + 配额 + 执行序
