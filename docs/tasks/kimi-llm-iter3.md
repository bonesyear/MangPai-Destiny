# Kimi 任务：LLM 通路 · prompt 迭代 3（空值键剔除 + 回对清单 + 禁下标硬注 + 复测）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/llm-batch-report-20260818.md` + 迭代 2 结果（L1 12.76% 剩 44 条：臆造键 ~20（guangming 误拼×3/bingxing/caifu_view.blockers×2）/空值键引用 ~18（hunyin.gong_attacked×5/zhiye.primary×3）/数组下标顽固 6（zhenbao-14a 占 5））
2. 本批 = **迭代 3**（三个方向全落地）+ 294 例复测；引擎零改动
3. 汇报 300 字内

## 任务（三个方向）
1. **清单剔除/标注空值键**：动态键清单生成时剔除空值键 or 标注「(空)」——治「键存在但空值」~18 条（LLM 引用空值键等于无出处）
2. **system prompt 回对清单**：basis 落笔前必须逐字回对键清单（「清单外键一律不写」从 user prompt 升级为 system 级硬约束）——治臆造键 ~20
3. **数组键行内硬注**：键清单里数组键行内加「禁止下标访问」（如 `juxiang[] 禁下标`）——治顽固 6 条

## 复测
- 294 例全量复测（validate=mark）
- 达标判定：L0≈0 / N1<2% / **L1<5%** / L2 参考
- 达标 → 正式通道判定（写进汇报）；不达标 → 剩余分布 + 收档判定（是否值得迭代 4）

## 红线
- 引擎零改动；只吃 trainset；LLM 输出不落 compute_all dict

## 验证
1. pytest 全绿
2. 复测达标判定表
3. 成本实测

## 汇报（300 字内）
三方向落地要点 + 复测达标判定（L0/N1/L1/L2）+ 剩余违规分布 + 成本 + **正式通道判定**
