# Kimi 任务：第五轮审查 V2 · 端到端真实路径审查（用户体验全链）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md` + 第五轮方案（`kimi-review5-plan-2026-08-20.md` 的 V2：端到端真实路径——真实使用场景全链审查；U2 遗留=真实凭证冒烟缺失，一直 mock 验证）
2. **独立判断纪律**：以真实行为为准
3. 本批 = **V2 端到端真实路径审查**（只审不改；**默认不连真实飞书/不调真实 DeepSeek**——除非需要验证的项必须真实凭证，则标注"需用户提供凭证"）
4. 汇报 300 字内

## 任务
1. **真实用户路径全链**（mock 飞书事件 + mock LLM，但走真实 engine）：
   - 用户发「阳历 1992-10-09 13:58 男 河南信阳」→ router 解析 → service 全链 → formatter 输出 → bot 回复
   - 覆盖：正常路径/畸形输入/无性别/边界年份/长文本/LLM 失败降级/并发两条消息
2. **失败路径检查**（V2 发布阻塞项=全链失败路径静默）：
   - 每个环节失败（解析失败/引擎异常/LLM 超时/发送失败）→ 用户侧看到什么（明确反馈 or 静默）
   - 判定：阻塞=有环节静默失败用户零反馈
3. **真实凭证冒烟评估**（U2 遗留）：
   - 评估当前 mock 验证 vs 真实飞书凭证冒烟的差距清单（哪些行为只有真实凭证能验：token 缓存/重试/消息格式真渲染）
   - 产出：真实凭证冒烟脚本（待用户提供 FEISHU_APP_ID/SECRET/VT 后一键跑）
4. **输出质量抽查**：3-5 个盘（不同八字结构）走完整链，检查 Markdown 输出渲染结构（标题/列表/强调）在飞书客户端的效果（已知飞书 Markdown 支持有限——检查有没有飞书不支持的语法）

## 红线
- 只审不改（问题清单，修复另排）
- 默认 mock（真实凭证冒烟=脚本产出待用户跑）

## 产出
1. 全链路径测试表（场景/结果/用户可见反馈）
2. 失败路径判定（哪个环节静默=阻塞）
3. 真实凭证冒烟脚本 + 差距清单
4. 输出渲染检查（飞书 Markdown 兼容性）
5. 汇报 300 字内
