# Kimi 任务：修复批 D6b · 子女断法实现（zinv 新维度，立 4 项）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md` + D6a 设计（`docs/kimi-d6a-zinv-design-20260819.md`：立法 4 项=得子应期窗/损子应期窗/借腹 marker/优劣时柱喜用腿归 liuqin 增补；收档 8 项不立；哨兵候选 6 条授课戊戌造价值最高；设计原则=zinv 只做岁运新维度、只读消费 liuqin 不重造、LLM 五维暂不扩）
2. **独立判断纪律**：旧归档仅参考；每条规则带书锚行号
3. 本批 = **D6b 实现**（立 4 项 + 哨兵先红后绿 + 六件套）
4. 汇报 300 字内

## 任务
1. **立 4 项**（按 D6a 设计，每条书锚行号随行注释）：
   - 得子应期窗（合动/开墓/制枭——案例≥3 同构）
   - 损子应期窗（克到位/合去/穿引动——引擎内部 marker，措辞避死亡词典交 `_scrub_death` 兜底）
   - 借腹 marker（两书同构）
   - 优劣时柱喜用腿（归 liuqin 增补）
2. **模块落位**：subjective/zinv 新模块（岁运新维度）+ engine 接线（若需要）；liuqin 增补
3. **哨兵先红后绿**：D6a 候选 6 条（授课戊戌造「一造三」优先）——先写断言（红）再改算法（绿）
4. **输出 schema**：进特征 JSON（LLM 五维暂不扩——不进 prompt，只进特征）

## 红线
- heldout 财 47✅/官 48✅/职 24✅ 不回退；引擎已有判定零回归（新维度增量，不改旧逻辑）
- 书锚铁律（每规则带行号）；只吃 trainset
- 措辞避死亡词典（_scrub_death 兜底）

## 验证（六件套）
1. 哨兵红绿（新增测试）
2. verify 432
3. pytest 全绿
4. blind --baseline snapshots/20260819_d3.json 零翻转（D4/D5 未动引擎，d3 仍是最新引擎基线）
5. 67/famous/calib 0 回归
6. 双 seed 一致

## 汇报（300 字内）
立 4 项实现/行号 + 哨兵红绿 + 输出 schema + 验证 6 项 + 零回归确认
