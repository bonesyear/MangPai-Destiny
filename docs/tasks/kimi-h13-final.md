# Kimi 任务：代码卫生审查 H13 · 汇总收官批（问题总账 + 修复批 H-fix 规划 + go/no-go）

## ⚠️ 执行指引
1. **先读** `/root/metaphysics/docs/knowledge-base.md` + 代码卫生规划 + H1-H12 报告（`docs/tasks/codehygiene-fix-backlog.md` 全部发现：P0×110 / P1×247 / P2×141 + 修复施工图（H11 _safe_compute 37 模块对照 + 三大函数拆分方案 + selectors 契约）+ 同型残留最终计数（裸 except 115/30 文件、原子写 12/11）+ 收工终态
2. 本批 = **代码卫生 H13 · 汇总收官批**（只审不改；纯本地零 API）
3. 汇报 300 字内

## 任务
1. **问题总账**（H1-H12 全量）：
   - 去重后总清单（H 批间重复项合并）
   - 按严重级 + 问题类型分组统计（裸 except/原子写/死代码/循环导入/命名/复杂度/重复——各类占比）
2. **修复批 H-fix 规划**（老规矩：依赖拓扑 + 分批）：
   - **H-fix 结构**：按依赖拓扑分批（如：①import/环境类 P0 先行（Optional/Any/Tuple——部署机崩溃面）②异常处理策略批（115 处裸 except 的系统性改造——按 _safe_compute 施工图：哪些改显式处理/哪些改传导/哪些该抛）③原子写批（12 处）④死代码/重复清理批（_check_pair/十神统一/死 shim）⑤大函数拆分批（gongliang/detect_relations/compute_all——按 H11 方案）⑥selectors 契约批⑦评测框架统一批（可选）⑧文档/基线同步批）
   - 每批：内容/工作量/验证（六件套红线——**引擎判定零改动是硬约束**，异常处理改造必须盲测零翻转）
   - 优先级（P0 部署机崩溃面先行）
3. **go/no-go 建议**：哪些批阻塞（P0 环境面/异常吞没面）、哪些可后置
4. **风险提示**：异常处理改造的风险（改"吞"为"抛"可能暴露新崩溃面——需要错误注入测试策略；拆分的回归风险——哨兵+六件套保障）

## 产出
1. 问题总账表（去重后）
2. H-fix 分批规划（每批内容/工作量/验证/优先级）
3. go/no-go 建议
4. 风险提示
5. 追加写入 `docs/tasks/codehygiene-fix-backlog.md`（H13 节）
6. 汇报 300 字内

## 红线
- 只审不改；纯本地零 API

## 汇报（300 字内）
问题总账去重统计 + H-fix 分批（依赖拓扑）+ 优先级/go-no-go + 风险提示
