# 第六轮审查 W1 · F1+F3 十一项代码审查报告（2026-08-22，只审不改，纯本地零 API）

> 对象：commit f3d3e5e 落地的 F1+F3 11 项。方法：当前代码逐项走查 + 死亡词 reject 边界 18 合成例实测（`_l2_enum` 直调）+ 降级链路模拟 + `test_f1_gate` 13 哨兵复跑（全绿）+ `_self_check` 离线复跑（OK）。
> 环境备注：本仓须用系统 `/usr/bin/python3`（3.14，PEP 649 惰性注解）；hermes venv 的 3.11 导入即 NameError（guanming.py:139 `Any` 未导入但 3.14 下不触发）——非本批审查面，仅记录防踩。

## 一、十一项逐项审查表

| # | 项 | 落点 | 判定 | 发现 |
|---|----|------|------|------|
| 1 | P0 免责声明 | formatter:16/:54（引擎直出尾）、llm_channel:386（LLM 叙述尾）、router:35-36（HELP 含隐私提示+免责） | ✅ 落点正确无缺场景（bot 用户面全闭合） | LLM 成功路径免责实际出现两次（段中+结尾），不碍事；render 层三条降级返回文本本身无免责行，靠 service 前缀兜底 → F6-4(P2) |
| 2 | P1 Tuple import | zuogong_detect.py:13 `from typing import ... Tuple`，:997 使用 | ✅ | — |
| 3 | P1 lark_md 三符 | formatter:51-52（`**` 粗体 + `· ` 圆点）；HELP 同口径 | ⚠️ 引擎直出路径干净；LLM 路径两处漏网 | format_reading 校验附注段 `  - [Lx]` bullets（llm_channel:377、:436）仍带 `- `；LLM conclusion 原文未做三符 sanitize → F6-5(P2) |
| 4 | P1 死亡词 mark→reject | llm_channel:429-433 | ✅ 行为正确（mark 模式拒出、附注不展示、service 降级引擎直出，哨兵绿） | reject 闸按 detail 子串「死亡红线」匹配而非结构字段 → F6-3(P2)；边界问题见第二节 |
| 5 | P1 外发告知 | README:81 + router HELP:35 | ✅ 双落点 | — |
| 6 | P1 丁眼锚注 | xiangmao.py:154（zhongji:2122-2147；gaoji:15337；…） | ✅ 锚注已补（书锚逐字回验归 W3） | — |
| 7 | F3 bot isinstance | bot.py:68 非 dict 静默丢弃；:71 encrypt 丢弃；token 校验 | ✅ | — |
| 8 | F3 S7 脱敏 | bot.py:53-55 通用信息不回显 str(e)；:121 500 不回显详情 | ✅（哨兵 test_respond_error_sanitized 绿） | — |
| 9 | F3 max_tokens 8192 | llm_backend.py:93 | ✅ 截断→JSON 必不合法→:425 降级链路闭合（无 partial JSON 兜底，不需要）；max_tokens 是上限非计费项，N2 ¥21.08/轮已含 8192 实证无额外上浮 | narrative.py:360 旧 Anthropic 通道仍 4096——非七维路径软依赖，不追 |
| 10 | F3 _self_check 人民币 | llm_backend.py:162-175 | ✅ 断言（峰 3.0/9.0、谷 1.5/4.5）与 _PRICE(:36-39) 逐值一致，离线跑 OK；format_reading:384 显示 ¥ 标签正确（字段名 cost_usd 滞后为已知文档项） | — |
| 11 | F3 L2 误报窗 | llm_channel.py:44-48、:289-294 | ⚠️ 窗边界有漏杀 | 句界符=`。！？!?\n`，逗号/分号不断句 → 见 F6-2(P1) |

## 二、风险面判定（#1/#2/#3/#5/#7/#8）

- **#1 reject L0/L2 层级混用 → 查实不成立（行为无混用）**。死亡词违规在 `_l2_enum` 标 L2（:295），但 reject 闸（:432）不按 layer、按 detail 子串「死亡红线」匹配，mark 模式拒出正常；:434 的 `layer=='L0'` 是 `validate='reject'` 模式的 schema 拦截，另一码事。L0 挂时 `_l2_enum` 仍全维扫描（合成例 15 证实迁移维死词照抓），无「L0 挂 L2 计数漏」。**残留**：子串匹配脆弱——未来任一检查 detail 含「死亡红线」四字即误拒 → F6-3(P2)。
- **#2 降级仅五段 → 属实**。reject/失败降级后 service 走引擎直出（formatter 做功/层功/三维/婚姻/应期+一句话），**迁移/相貌两维丢失**，与正常七维不一致；且降级提示语一律「LLM 通道暂不可用」，死亡词拒出场景文案误导 → F6-1(P1，维度数是否可接受需裁定）。
- **#3 降级免责 + prompt 外泄 → 用户面闭合**。service 前缀白名单（:22）吞掉 `[LLM 不可用`/`[LLM 输出非合法 JSON`/`[断语被` 三类，SYSTEM prompt（含 fewshot）与 raw output 均不到飞书用户；降级成品=引擎直出带免责 ✓。直接 CLI 调 `render_structured_reading` 无免责且泄 prompt——docstring 已声明为调试契约，非用户面。**残留**：闭合完全依赖 service 一处前缀兜底，新消费方绕过即裸奔 → F6-4(P2)。
- **#5 误报窗边界 → 有漏杀**。窗=同句（句界符 `。！？!?\n`）；**逗号/分号不断句**：合成例 6「你寿数将尽，但我不予置评」、例 10「享年六十八，此类寿数我不测」均被误豁免放行。ponytail 注释（:46）已认领语义级残留人工兜底，但此族是结构性窗口问题非语义级 → F6-2(P1)。
- **#7 lark_md `·` 渲染**：`· ` 非 markdown 标准符，按纯文本字面渲染（设计即纯文本兜底，可接受）；`**` 粗体 lark_md 支持；`---` 已去净。真正漏网在 LLM 路径（见 #3 项/F6-5）：附注 bullets `- ` + LLM 原文未 sanitize → P2 cosmetic。
- **#8 _self_check 人民币口径 → 一致**。断言与 `_PRICE` 逐值相符（峰 3.0/9.0、谷 1.5/4.5），离线自检 OK，无误挂/漏挂。

## 三、reject 边界合成例实测（18 例，`_l2_enum` 直调）

| # | 例 | 预期 | 实测 |
|---|----|------|------|
| 1 | 正常句 | 放行 | ✅ pass |
| 2 | 裸死词「六十八岁寿终」 | 拒 | ✅ REJECT |
| 3 | 合规拒答同句「不予断言寿数」 | 豁免 | ✅ pass |
| 4 | 拒答标记跨句号 | 拒 | ✅ REJECT |
| 5 | 拒答标记跨换行 | 拒 | ✅ REJECT |
| 6 | 拒答标记仅逗号分隔 | 应拒 | ❌ **pass（漏杀）** |
| 7 | 感叹号分隔 | 拒 | ✅ REJECT |
| 8 | 英文 death/lifespan | 放行（可接受，中文输出面） | ✅ pass（P2 观察） |
| 9 | 近义词「离世/归西」 | 应拒 | ❌ **pass（词表缺口）** |
| 10 | 先断后拒同逗号句「享年六十八，此类寿数我不测」 | 应拒 | ❌ **pass（漏杀）** |
| 11 | 迁移维「出国」 | L2 违规 | ✅ 抓（非死词道） |
| 12 | 相貌「美元」 | 豁免 | ✅ pass |
| 13 | 相貌「乙丑时」 | 豁免 | ✅ pass |
| 14 | 相貌「秀美」 | L2 违规 | ✅ 抓 |
| 15 | 死词在迁移维（全维扫描） | 拒 | ✅ REJECT |
| 16 | 书引含「归西」+「短命」 | 拒 | ✅ REJECT（靠「短命」） |
| 17 | 句首死词无前界 | 拒 | ✅ REJECT |
| 18 | 死词句末无句号 | 拒 | ✅ REJECT |

15/18 符合预期；3 例偏差聚两类：逗号同句误豁免（6/10）、死词近义词表缺口（9，「离世/去世/归西/病逝」等常见词不在 11 词黑名单——注意 scrub 守输入 + prompt 正向处方在前，黑名单本是兜底，但仍建议增补）。

## 四、P0/P1/P2 清单（修批 G 候选）

**P0：无。**

**P1**
- F6-1 reject/失败降级路径维度不一致：引擎直出仅五段，迁移/相貌丢失（风险 #2）；降级提示语在死词拒出场景误写「暂不可用」。需裁定：可接受则改提示语区分场景，不可接受则 formatter 补两维。
- F6-2 误报窗结构性漏杀：逗号/分号不断句致「死断+拒答标记同逗号句」误豁免（风险 #5）；死词黑名单缺常见近义词（离世/去世/归西/病逝）。修法候选：句界符补「，；」或限定拒答标记须在死词前；词表增补。

**P2**
- F6-3 reject 闸按 detail 子串「死亡红线」匹配（风险 #1 残留），改结构化标记（如 violation 加 `reject: true` 字段）防未来误拒。
- F6-4 render 层三条降级返回文本（:412/:419/:425/:433）本身无免责行，用户面闭合单点依赖 service 前缀白名单（风险 #3 残留）。
- F6-5 lark_md LLM 路径漏网（风险 #7）：format_reading 附注段 `  - [Lx]` bullets 仍带 `- `；LLM conclusion 原文未做三符 sanitize。
- 观察项（不列修）：英文死词不覆盖（中文输出面，可接受）；LLM 成功路径免责出现两次（段中+结尾）；hermes venv py3.11 与本仓不兼容（PEP 649）。

## 五、验证记录

- `python3 -m pytest mangpai/tests/test_f1_gate.py` → **13 passed**（现役哨兵全绿）
- `python3 -m mangpai.subjective.llm_backend` → self-check OK
- 合成例 18 例 + 降级链路模拟（reject 无免责/降级泄 prompt/service 前缀吞没/附注 `- ` 残留）均实测确认
